 <!DOCTYPE html>
 <html lang="en">
 <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Main Page</title>
    <!-- Dont use /public/stylesheets/style.css because we have already setup app.use(express.static(path.join(__dirname, 'public'))); -->
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
 </head>
 <body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <form action="/create" method="POST">
                <input class="block px-5 py-2 bg-zinc-800 w-full rounded-md" type="text" placeholder = 'Title goes here' name="title">
                <textarea class="block px-5 py-2 mt-2 bg-zinc-800 resize-none w-full rounded-md " placeholder = 'Description goes here' name="description"></textarea>
                <button class="mt-2 bg-blue-600 px-3 py-1 rounded-md " type="submit">Save</button>
            </form>
        </div>
        <div class="cards flex flex-wrap items-center justify-center">
            <% if(files.length > 0){ %>
                <% files.forEach(file => {%>
                    <div class="task min-w-72 m-2 px-3 py-4 bg-zinc-700 rounded-md relative">
                        <h1 class = "text-white text-2xl tracking-tighter"><%=file.replace('.txt','').replaceAll('-',' ')%></h1>
                        <div class="text-blue-500 flex w-full justify-between mt-3">
                            <a href="/file/<%= file%>" class="inline-block">Read More</a> 
                            <a href="/edit/<%= file%>" class="inline-block">Edit</a>
                        </div>
                        <a href="/delete/<%= file%>"><button class="absolute top-1 right-1 bg-red-500 text-xs rounded-md p-0.5">Delete</button></a>
                    </div>
                <% }) %>
            <% } else {%>
                <h3 class="text-zinc-600 m-2 px-3 py-4">No notes to display</h3>
            <% } %>            
        </div>
    </div>

    <script src="/javascripts/script.js"></script>
 </body>
 </html>